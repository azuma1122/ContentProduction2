using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Game.StageScene.Magnet
{
    /// <summary>
    /// 磁石の極タイプを共通管理する列挙型
    /// - N極: North（赤）
    /// - S極: South（青）
    /// </summary>
    public enum MagnetPoleType
    {
        North,  // N極
        South   // S極
    }
}
    public class MagnetPoleType : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
